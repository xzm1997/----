const asyncPool = function () {
  
}

// Test
const targetFn = function () {
  return new Promise((resolve, reject) => {
    setTimeout(resolve(i), i);
  })
}

const main = async function() {
  return resAll = await
}
