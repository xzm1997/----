function chainNode(val) {
  return {
    val: val,
    next: null
  }
}

function game(num) {
  // 边界条件
  if (num < 1) return null;
  if (num === 1) return 1;
  // 链表建立
  for (let i = 1; i <= num; ++i) {

  }
  // 运行
  // 返回结果
}
