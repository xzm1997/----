function winMazeGift( maze ) {
  // write code here
  let res = [];
  // find entry
  let entry = [];
  for (let i = 0; i < maze.length; ++i) {
    if (maze)
  }
}

let valReg = /[a-zA-z\s]+/;
console.log(!valReg.test('a'));
